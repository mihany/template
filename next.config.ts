import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // images: {
  //   remotePatterns: [
  //     // Allow images from localhost for development
  //     {
  //       protocol: "http", // Use "http" for localhost
  //       hostname: "localhost",
  //       port: "3000", // Specify the port if running locally (adjust if necessary)
  //       pathname: "/images/**", // Match the local image path
  //     },
  //     // Allow images from your live website
  //     {
  //       protocol: "https",
  //       hostname: "mihany.github.io",
  //       pathname: "/template/images/**", // Adjust the path to your live image directory
  //     },
  //   ],
  // },
  // Uncomment for static export (if needed):
  // output: "export",

  /* other config options */
};

export default nextConfig;